import React from 'react';

export default function AccountDashboardPage() {
  return (
    <div style={{ padding: '2rem', textAlign: 'center' }}>
      <h2>แดชบอร์ดบัญชีผู้ใช้ (Account Role)</h2>
      <p>หน้านี้สำหรับบัญชี role: account (เวอร์ชันเปล่า)</p>
    </div>
  );
}
